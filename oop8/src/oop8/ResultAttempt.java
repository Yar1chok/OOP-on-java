package oop8;

/**
 * Represents attempt result
 */
public enum ResultAttempt {
    /**
     * This enum stores options for passing the exam by a student: Passed, Failed and Retry(come up again).
     */
    PASSED, FAILED, RETRY;
}
