public enum menu_options {
    ENTER_TEXT(1), RANDOM_TEXT(2), SHOW_TEXT(3), REVERSE_TEXT(4), SHOW_REVERSE(5), EXIT(6);

    final int number;
    menu_options(int number) {this.number = number;}
}
